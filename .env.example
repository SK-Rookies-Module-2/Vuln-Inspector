# 데이터베이스 연결 정보
DB_USER=vuln
DB_PASSWORD=vuln
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vuln_inspector

# DATABASE_URL을 직접 지정하면 위 설정을 덮어씁니다.
# DATABASE_URL=postgresql+psycopg://vuln:vuln@localhost:5432/vuln_inspector
